import React, { Component } from 'react';


const SurvivorResources = (props) => {
    return (
    <h2>
        Resources will be added in the next iteration. This current version is just for testing the matches and settings portion of our application.
    </h2>
    );
}
 

export default SurvivorResources;